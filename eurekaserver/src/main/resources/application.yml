server:
  port: 8761

eureka:
  instance:
    #hostname: localhost
    # 可以让docker容器名代替主机名被访问
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    #serviceUrl:
    #  defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

#spring:
#  application:
#    name: eurekaserver